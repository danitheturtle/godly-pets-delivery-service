At the most basic form the player could control a platform between either an up or down position, like an elevator.

In a more complex scenario, the platform could have multiple pre-determined "nodes" it stops at